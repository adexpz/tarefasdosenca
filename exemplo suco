decimal suco, quantidade, suco2;

Console.WriteLine("digite quantos sucos irar querer");
quantidade  = Convert.ToInt32(Console.ReadLine());

suco = 5.50M;
suco2 = 4.50M;
if (quantidade >= 10)
{
    suco2 = 4.50M * quantidade;
    Console.WriteLine("isso custara " + suco2);

}
else
{
    suco = 5.50M *quantidade;
    Console.WriteLine("isso custara "+ suco);
}
