using System.ComponentModel.Design;
using System.Runtime.Intrinsics.Arm;
using System.Security.Principal;

//filar exemplo 3

Console.WriteLine("digite seu nome");
string nome = Console.ReadLine();

Console.WriteLine("O atendimento e prioritario?:");
string prioritario = Console.ReadLine();




if (prioritario.ToLower() == "sim")
{
    Console.WriteLine("Certo " + nome + ", vai para a fila 1, 2, 3, Elas Sao prioritarias");

}
else
{
    Console.WriteLine("certo " + nome + ",vai para qualquer fila exeto 1,2,3 elas sao prioritarias");

}
